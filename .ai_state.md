# Project Objective: Русская Рамаяна — портал, архив переводов и краудфандинг; приведение к DH-стандартам по docs/DH_ROADMAP.md

## ➡️ Next Steps (Queue)

- [ ] 2026-08-14: **Litpam-Indexator print-readiness — остаток после H2589.**
      [H2588 (Fable 5) — общий print-контракт четырёх указателей книг I–II](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H2588-Fable_RussianRamayana_litpam-index-style-print-contract_12.08.26.md) ✅;
      [H2589 (Sonnet 5) — deterministic tooling и print-readiness пилот книги I](https://github.com/gasyoun/Uprava/blob/main/handoffs/H2589-Sonnet_RussianRamayana_litpam-book1-print-readiness-pilot_12.08.26.md) ✅
      (статус `HUMAN_REVIEW_WAITING`, conversion gate книги I = **FAIL**: overset в 3 историях
      DFT-I-0002 + 6 missing links DFT-I-0001 — см. «Результат H2589» в
      [`PLAN_LITPAM_INDEXATOR_PRINT_READINESS_2026.md`](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/docs/PLAN_LITPAM_INDEXATOR_PRINT_READINESS_2026.md)).
      **Обновление 15-08-2026 (H2770, Fable 5 `claude-fable-5`):** InDesign-проверка больше
      НЕ нужна — overset адъюдицирован детерминированно ([PR #66](https://github.com/gasyoun/RussianRamayana/pull/66)):
      0/442 страниц отличий 2022↔2026, все 3 overset-истории безобидны (тегированная
      рабочая история `c-`/`d-`, не Именной указатель), DFT-I-0002 blocker→cosmetic —
      [адъюдикация](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/artifacts/print-readiness/book-I/overset-adjudication-2026/OVERSET_TEXTDIFF_ADJUDICATION_BOOK_I_2026.md).
      **Waiver ПРИНЯТ МГ 15-08-2026, шаг 6 ЗАПУЩЕН (H2776, Fable 5 `claude-fable-5`):**
      гейт `PASS_WITH_WAIVERS`; pilot workspace + версионная копия `Ramayana_I_pilot_2026.indd`;
      overset 3→0 (extension-фреймы, страницы посимвольно идентичны 0/442); corrected
      workbook подключён. Операторская половина (стадии `[1]`–`[4]`, ≈3–6 ч) — по
      [PILOT_BOOK_I_OPERATOR_RUNBOOK_2026.md](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/docs/print-readiness/PILOT_BOOK_I_OPERATOR_RUNBOOK_2026.md).
      **15-08 (продолжение): строка 221 закрыта рулингом (43/43, PR #68); стадия `[1]`
      выполнена агентом через COM (MG-override)** — сводная `IndexList[@]001.indd`
      1498 строк в pilot-workspace, сравнение с 2025:
      [вердикт](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/artifacts/print-readiness/book-I/pilot-2026/stage1/SVODNAYA_VS_2025_STAGE1_COMPARISON.md)
      — потерь нет, но ~15–20 терминов 2025 отсутствуют в словнике (2025 log-triage
      добавлял их прямо в вёрстку; backfill-канал — решение человека).
      **15-08 (финал): backfill (а) 18 терминов → словник `…08_15b.xlsx`; стадия `[1]`
      пересобрана (сводная 1516, PR #70); стадия `[3]` ВЫПОЛНЕНА (1318 topics, PR #71;
      триаж лога: 133 ожидаемых + 5 подозрительных + 0 ошибок; три DOM-регрессии
      InDesign 2026 задокументированы, аддитивный `index_letter.jsx`); evidence 442 стр.,
      OVERSET=0. H2776 закрыт частичной поставкой по указанию МГ.** Остаток: стадия `[4]`
      (сборка + оформление по INDEX_STYLE_SPEC) + финальный packet — в волне
      [H2590 (Sonnet 5) — print-readiness книги II](https://github.com/gasyoun/Uprava/blob/main/handoffs/H2590-Sonnet_RussianRamayana_litpam-book2-print-readiness-application_12.08.26.md)
      (гейт снят) либо оператором.
      **16-08-2026 (H2590, Sonnet 5 `claude-sonnet-5`): книга II запущена.** Baseline-2022
      (статический аудит, InDesign 2022 без COM ProgID на этой машине) + живой 2026 COM
      конверсия (668 стр., 53 истории); Step5 gate FAIL те же классы дефектов, что книга I
      (DFT-II-0001: 7 LINK_MISSING, 3/5 имён совпадают с книгой I; DFT-II-0002: 3 overset).
      Адъюдицировано `overset_textdiff.py` (0/668 страниц отличий, все 3 истории рендерятся
      100%) — [адъюдикация](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/artifacts/print-readiness/book-II/overset-adjudication-2026/OVERSET_TEXTDIFF_ADJUDICATION_BOOK_II_2026.md).
      МГ применил waiver-прецедент книги I (H2776) → `PASS_WITH_WAIVERS`
      ([PR #75](https://github.com/gasyoun/RussianRamayana/pull/75), merged). Pilot workspace
      `work/print-readiness/pilot-II/Ramayana_II_pilot_2026.indd` (вне Git) из уже
      сконвертированной 2026-копии + подключён corrected workbook `…08_15b.xlsx`.
      **Стадия `[1]` выполнена** (`drive_stage1.py`, COM): все 4 листа словника →
      IndexList-001..004.indd (784/201/443/88 строк — те же числа, что книга I, общий
      словник). Заметно медленнее книги I на этой машине (Именной ~50 мин вместо 30,
      Предметы ~35 мин вместо 12 — вероятно машинная/InDesign-инстанс нагрузка, не размер
      данных: счёт строк почти идентичен книге I). Фоновый драйвер-процесс несколько раз
      получал `killed` (сессионный лимит времени вызова), но сам InDesign продолжал
      выполнение ExtendScript сервер-side независимо от клиента — прогресс не терялся,
      просто требовалось вручную возобновлять цикл по листам после каждого разрыва.
      **Стадия `[1b]` выполнена** (`drive_stage1b.py`): маркеры a/b/c/d + 3 мерджа
      (43с/101с/31с, все "Таблица добавлена") → сводная `IndexList[@]001.indd`
      (work/print-readiness/pilot-II/indexlists/, вне Git). Остаток: стадия `[3]`
      (`drive_stage3.py`, ожидать те же DOM-регрессии InDesign 2026, что H2776
      задокументировал для книги I) → стадия `[4]` (сборка/оформление по
      INDEX_STYLE_SPEC.md, GUI-зависимая) → финальный evidence packet.
      **16-08-2026 (продолжение): стадия `[3]` — заблокирована средой на этой машине,
      НЕ данными/логикой.** 20-строчный тест letter `a`: 3.0 мин / 20 строк, 83 записи
      найдены — измеренный per-term cost, не зависание (экстраполяция на весь letter `a`,
      784 строки, ≈2 ч). Полный прогон реально занял ≥5.6 ч без завершения (стабильный
      рост CPU всё время, `Responding=True` — не завис, просто медленнее оценки).
      Построен [`drive_stage3_checkpointed.py`](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/tools/indesign/drive_stage3_checkpointed.py)
      (аддитивный, поверх `drive_stage3.py` без изменений авторских скриптов): RUN+save
      склеены в ОДИН DoScript-вызов (сохранение переживает убийство orchestrating
      Python-клиента сессионным лимитом — измерено, InDesign продолжает ExtendScript
      сервер-side независимо), плюс `--chunk-size`/`--start-offset` для мелкогранулярного
      чекпоинта (по умолчанию отключено, `--chunk-size 100` не испытан вживую).
      **Блокер — не данные:** каждый раз, когда InDesign-приложение (не просто Python-
      клиент) убито force-kill'ом, следующий запуск виснет на модальном диалоге
      восстановления файла, требующем ручного клика на физической машине (COM
      `UserInteractionLevel` установить нельзя, пока диалог активен — курица-яйцо).
      Машина активно используется человеком + несколькими параллельными agent-сессиями
      одновременно (Grok/Codex/другой Claude Code) — экран занят, несколько попыток
      дозвониться прошли не полностью. **Правило на будущее: НИКОГДА не `Stop-Process
      -Name InDesign` напрямую** — только естественная гибель orchestrating Python-
      процесса по сессионному лимиту, которую InDesign переживает без диалога. Текущий
      pilot-II/Ramayana_II_pilot_2026.indd не заменён (файл занят диалогом) — состояние
      неизвестно/неподтверждено, для чистого возобновления пересоздать SHA-verified
      копию из `work/print-readiness/book-II/production-2026-workcopy/Ramayana_II_conversion_2026.indd`
      (567b30a5583f21f9d51c2772cf8673563c987579337f33975ec615e98830216d) ПОСЛЕ того как
      диалог снят и документ закрыт чисто через COM (`Close(idNo)`), не убийством
      процесса. Дальше: запустить `drive_stage3_checkpointed.py --chunk-size 100` без
      `--letters` (все 4 буквы) и просто терпеливо ждать/резюмировать по чекпоинтам —
      не убивать InDesign вручную. Инструмент влит
      [PR #77](https://github.com/gasyoun/RussianRamayana/pull/77). Статус для Анатолия
      (сделано/осталось/блокер/как продолжить) —
      [issue #78](https://github.com/gasyoun/RussianRamayana/issues/78).
      **17-08-2026: стадия `[3]` ЗАВЕРШЕНА, обходом сломанной палитры.** После
      перезапуска машины InDesign сам упал/закрылся (не по моей команде — чисто,
      без диалога при следующем старте); файл `pilot-II/Ramayana_II_pilot_2026.indd`
      сохранил прогресс буквы `a` (761 topic — та же цифра, что книга I). Прогон
      авторской палитры на письме `b` воспроизвёл ТУ ЖЕ DOM-регрессию InDesign 2026,
      что H2776 задокументировал для книги I (`ProcStoryOrDoс`, строка 773, «Invalid
      parameter», для любого диапазона не с нулевой строки). Решение — тот же
      санкционированный «additive equivalent wrapper», что уже проверен на книге I:
      [`index_letter.jsx`](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/tools/indesign/index_letter.jsx)
      (не менялся) через новый параметризованный драйвер
      [`drive_stage3_own_checkpointed.py`](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/tools/indesign/drive_stage3_own_checkpointed.py)
      (RUN+save в одном DoScript-вызове, как и checkpointed-версия палитры).
      **Результат: b=944 (4.9 мин), c=1230 (22.6 мин), d=1318 (8.8 мин) — итого
      1318 topics (a=761/b=183/c=286/d=88), ТОЧНОЕ совпадение с итогом книги I.**
      Независимо перепроверено повторным открытием файла в режиме только-чтения.
      Метод оказался значительно быстрее сломанной палитры (найдено ~1.5–3 с/строка
      против ~9 с/строка). **Лог «не найдено» (255 записей) триажирован**
      ([`analyze_stage3_log_book2.py`](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/tools/analyze_stage3_log_book2.py),
      база сравнения — собственный не-найденный список книги I, а не diff
      стадии [1], которого у книги II нет): **233/255 (91 %) ожидаемые**
      (книга I их нашла — термины принадлежат тексту тома I общего словника),
      **22/255 (9 %) подозрительные** (не найдены ни в одном томе — тот же
      класс, что 5 подозрительных книги I: многословные составные записи и
      общая ритуально-мифологическая лексика, вероятный пробел в формах
      поиска словника, не системная ошибка) —
      [триаж](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/artifacts/print-readiness/book-II/pilot-2026/stage3/STAGE3_LOG_TRIAGE.md).
      **Стадия `[4]` начата: шаг «Построить указатель» (нативная InDesign-функция,
      не авторский скрипт).** API — `Index.generate(on, placePoint, destinationLayer,
      autoflowing, includeOverset)`, все параметры опциональны, подтверждено
      рефлексией живого объекта (`idx.reflect.methods`) + внешней документацией
      ExtendScript API (indesignjs.de) — НЕ угадано. Инструмент
      [`generate_index.py`](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/tools/indesign/generate_index.py)
      добавляет новую страницу В КОНЕЦ документа (оригинальные страницы 2025 года
      не трогает, рулинг 26) и вызывает `generate()` с `autoflowing=true` (InDesign
      сам создаёт следующие страницы при переполнении). **Инкрементальное
      тестирование подтвердило: `generate(page)` / `generate(page,point)` /
      `generate(page,point,layer)` — все работают быстро; 4-й аргумент
      (`autoflowing=true`) — единственный, где происходит реальная раскладка
      контента (~4 мин по MANUAL, подтверждается: устойчивый рост CPU, не завис).**
      Первые два прогона `generate_index.py` упали ДО фикса: (1) `newPage.layers`
      → надо `doc.layers` (страница не имеет `.layers`, только документ) — исправлено;
      (2) `"undefined is not an object"` уже ПОСЛЕ фикса — по инкрементальным тестам
      сам `generate()` не виноват; причина подтверждена третьим прогоном —
      недоказанное имя свойства `story.textContainers` при сборке строки результата
      (не критично, `doc.save()` в JSX идёт ДО этого чтения). **✅ Построить
      указатель — ГОТОВО и подтверждено:** третий прогон (с фиксом `doc.layers` +
      defensive-свойствами) завершился успешно за 1.7 мин,
      `pages_before=705|pages_after=742` (+37 новых страниц под указатель,
      `autoflowing=true` сработал). Независимо перепроверено повторным открытием:
      742 страницы, 54 истории (было 53 — новая история указателя), последняя
      история — 50 180 символов, начинается «Сводный указательRomanAa-Агастья
      159, 225, 534, 594, 609, 613, 614, 630a-Агни …» — реальный контент указателя
      с корректным форматом (буква-маркер + локаторы через запятую).

      **Полная процедура стадии `[4]` после «Построить указатель»** (MANUAL, раздел
      «Стадия [4]», [docs/indesign-pipeline/MANUAL.md#стадия-4](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/docs/indesign-pipeline/MANUAL.md)):
      1. ✅ ГОТОВО — Построить указатель (742 страницы, 50 180 символов, подтверждено).
      2. `ProcNumberLines[3-4,6-8].jsx` (папка «Оформление номеров страниц в указателе») —
         свёртка подряд идущих номеров в диапазоны через дефис + полужирные ссылки
         примечаний (диапазон страниц примечаний — `notes_bold_page_ranges` в
         `config/print-readiness.json`, для книги II ещё **не заполнен**, `null` —
         нужно измерить с baseline IDML/PDF аудита прежде, чем полагаться на
         полужирность §7.3 spec).
      3. `SplitStory` (папка «Useful Support Tools», НЕ в `[4. Оформление указателя]`) —
         разрезает сводный сгенерированный указатель по буквам-маркерам `A`/`B`/`C`/`D`
         (заглавные — проверить регистр против канона `a`/`b`/`c`/`d` из
         `ForIndex.jsxinc`), удаляет служебные префиксы поиском-заменой.
      4. Остальные операции (порядок — в `[4. Оформление указателя]/Очерёдность.txt`,
         cp1251): `AddSeeTopic.v.3.v.3.jsx` («См.», модель тома II §7.5),
         `HideShowNumber.v.2.jsx` (скрыть служебные `a-`/`b-`/…), `AddAnnotationData.v.3.jsx`
         (аннотированные подстатьи). `AddLetter.v.2.jsx` (алфавитные рубрики) и
         `DashInsteadWord.jsx` (прочерки) — **ВЫКЛючены контрактом** (§7.6 spec,
         0 в обоих томах в печати 2025) — НЕ запускать.
      5. Как и стадия `[3]`: если авторский скрипт упадёт на DOM-регрессии InDesign
         2026 (тот же класс, что H2776/`ProcStoryOrDoс` и сегодняшний `layers`-баг) —
         не чинить авторский `.jsx`, писать additive-эквивалент (как `index_letter.jsx`),
         тестировать ИНКРЕМЕНТАЛЬНО (по одному аргументу/свойству, не всё сразу) —
         подход из сегодняшней отладки `generate()` экономит часы против слепых
         повторов всего скрипта.
      6. Каждый шаг — checkpoint-save в ОДНОМ DoScript-вызове с самим действием
         (см. `build_run_and_save`/`build_index_letter_and_save` в
         `drive_stage3_checkpointed.py`/`drive_stage3_own_checkpointed.py` —
         паттерн, спасший прогресс стадии `[3]` от сессионных обрывов Python-клиента).

      Финал стадии `[4]`: замена старых страниц указателя 2025 года (630–649 книги II
      по PLAN) на новые сгенерированные — редакционное решение о финальной вёрстке
      (пагинация, разворот), НЕ решать молча; после — coverage/reference recheck
      100%, versioned IDML + proof-PDF + evidence packet + review checklist.

      **19-08-2026 (H2590, Sonnet 5 `claude-sonnet-5`): контаминация stage[3] книги II
      исправлена и подтверждена (продолжение 17-08 находки/фикса, PR #86).** Редо
      букв `b`/`c`/`d` через `index_letter.jsx --exclude-from-page 630`
      (`drive_stage3_own_checkpointed.py`): 2.0/9.1/1.8 мин, все checkpoint-saved;
      17 хитов исключены фильтром по всем 4 буквам. Подтверждено на примере:
      `a-Агастья` печаталась `…613, 614, 630` (контаминация) → теперь `…613–614`
      (630 корректно исключён). Документ перед перегонкой: 668 стр./52 истории —
      точное совпадение с committed baseline `idml-audit.json`; старые сгенерированные
      страницы указателя (742 стр. из прошлого прогона) на диске отсутствовали
      (вероятно, не пережили ранее задокументированный сбой/диалог восстановления
      InDesign), но source-of-truth `Index.Topics` (935 топиков после фикса) —
      целы, потерь не было. Stage[4] шаги 1–2 перегнаны с нуля на исправленных
      данных: «Построить указатель» (668→711 стр.) + `ProcNumberLines` (1.9 мин).
      **Coverage-проверка v2** (`dump_topic_pages.py`+`analyze_topic_pages.py`,
      множества страниц — `coverage_check_stage4.py` v1 устарел, ложно флагует
      легитимную дедупликацию) нашла **новый дефект `DFT-II-0003`** (material,
      `WAITING`, [defect-ledger.json](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/artifacts/print-readiness/book-II/defect-ledger.json)):
      29/934 топиков теряют ровно один локатор в печати. Гипотеза: у 693/934
      топиков есть один «хвостовой» `PageReference`, невалидный после
      `ProcNumberLines` (`Object is invalid`) — для 664/693 безвредно (страница
      уже в свёрнутом диапазоне), для 29/693 осиротевшая ссылка держала
      единственное вхождение непоследовательной страницы, молча пропадающей из
      печати. Поведение нативной палитры `ProcNumberLines`, не `index_letter.jsx`.
      **Книга I ещё не проходила stage[4] — вероятно тот же риск, вне рамок этого
      хендоффа** (заголовок «книга II»), отдельный follow-up для человека/нового
      хендоффа. Остаток: разобраться в `DFT-II-0003` (или принять WAITING и
      продолжить) → `SplitStory` → «См.»/`HideShowNumber`/`AddAnnotationData` по
      авторскому `Очерёдность.txt` (`AddLetter`/`DashInsteadWord` — выключены
      контрактом); `notes_bold_page_ranges` для I/II — всё ещё `null` (не
      сделано в этой сессии).

      **19-08-2026 (продолжение): DFT-II-0003 закрыт рулингом МГ** («принять,
      29 строк в чек-лист корректуры, не гнаться за фиксом внутри
      `ProcNumberLines`») — [DFT-II-0003_PROOFREADING_LIST.md](https://github.com/gasyoun/RussianRamayana/blob/main/Litpam-Indexator/artifacts/print-readiness/book-II/pilot-2026/stage4/DFT-II-0003_PROOFREADING_LIST.md),
      ledger disposition → `queued-for-manual-proofreading`.
      **`SplitStory`-эквивалент выполнен**: вместо интерактивного авторского
      `SplitStory[move].jsx` (требует ручное GUI-выделение на стыке фреймов)
      применён `paragraph.startParagraph = StartParagraph.NEXT_PAGE`
      (подтверждено рефлексией enum, не угадано) на границах `b-`/`c-`/`d-` —
      реализует `section_start.fresh_page_required: true` напрямую, без
      вставки/удаления текста. Подтверждено: `storyLength` 49929→49929
      (без изменений), без overset, страницы границ сдвинулись
      (694→695/698→700/706→708), итог документа как был — 711 стр.
      Coverage v3 даёт ТОТ ЖЕ набор 272 несовпадений, что и v2 (0 новых
      регрессий, 0 исчезнувших) — разбивка не потеряла и не задвоила ссылок.
      Остаток: заголовки 4 указателей (текст+стиль по `config.indexes_order`/
      §3 spec, стиль заголовка ещё не идентифицирован) → «См.»
      (`AddSeeTopic.v.3.v.3.jsx`) → скрытие служебных `a-`/`b-`/`c-`/`d-`
      (`HideShowNumber.v.2.jsx`) → `AddAnnotationData.v.3.jsx`.

      **19-08-2026 (продолжение 2): заголовки 4 указателей сделаны.**
      Находка: `Index.generate()` сам вставил 4 однобуквенных параграфа
      «A»/«B»/«C»/«D» (стиль `Index Section Head`, 12pt Bold) ровно на
      границах разделов — не то же самое, что запрещённые `letter_rubrics`
      (те про алфавитные суб-рубрики ВНУТРИ указателя, тут 0). Собственный
      баг того же захода: `NEXT_PAGE` стоял на первой СТАТЬЕ раздела, а не
      на ЗАГОЛОВКЕ — заголовок сиротел на предыдущей странице. Исправлено
      робастно (по индексу параграфа, не по статичному смещению символа):
      заголовки получили текст из `config.indexes_order` (естественный
      регистр, как в оригинале на стр. 630) + стиль `Общее название
      указателя` (**14pt Bold — подтверждено рефлексией, точное совпадение
      с §3 spec тома II**, не угадано) + `NEXT_PAGE`; статьи вернулись на
      `ANYWHERE`. Проверено: storyLength 49929→50070 (рост от текста
      заголовков), без overset, все 4 заголовка — первый символ своего
      фрейма (стр. 670/696/701/709). **Coverage v4 = тот же набор 272
      несовпадений**, 0 новых регрессий. Остаток: `para[0]` «Сводный
      указатель» (нативный колонтитул от `Index.generate()`, судьба не
      решена) → «См.» → скрытие `a-`/`b-`/`c-`/`d-` →
      `AddAnnotationData.v.3.jsx`.

      **19-08-2026 (продолжение 4): «См.»-ссылки сделаны — не через
      авторский GUI-скрипт (интерактивная палитра, редакционный выбор
      "кому нужна ссылка" не выводится механически из словника), а через
      ground truth из УЖЕ ПРОВЕРЕННОЙ печати 2025 г.** Извлечено 118 строк
      «Алиас см. Цель» из `Ramayana_II_12.10.25.pdf` стр. 630–649 (pymupdf)
      — точное совпадение с задокументированным `volume_II_2025_observed_
      count: 118`. Сверено с текущей моделью `Index.Topics`: 109/118 чистых
      + 6 исправлено (PDF-перенос строк) + 2 исключено (не алиасы) = **115
      верифицированных пар**. Позиция вставки — по нативно отсортированному
      `ix.topics` (не по плоскому regex, подстатьи печатаются по дереву, не
      алфавиту — первая попытка ошибалась). 115/115 вставлены без ошибок,
      побочный overset (+2629 симв.) исправлен добавлением 2 страниц
      (711→713). Верификация ПОПУТНО нашла и закрыла баг
      `analyze_topic_pages.py` (regex-lookahead не знал про «см.»-строки —
      44 ложных «not found»); после фикса v5(до)/v6(после) дают
      ИДЕНТИЧНЫЕ 270 несовпадений (даже на 2 меньше исходных 272 — фикс
      убрал 2 baseline-ложняка) — 0 регрессий, 0 потерь. Остаток: скрытие
      `a-`/`b-`/`c-`/`d-` (`HideShowNumber.v.2.jsx`) →
      `AddAnnotationData.v.3.jsx` → финал.

      **19-08-2026 (продолжение 5): служебные префиксы `a-`…`d-` удалены
      (не через `HideShowNumber.v.2.jsx` — тот скрипт нацелен на хвостовые
      номера-разделители омонимов, не на ведущие буквенные маркеры; PASS-
      условие spec выполнено напрямую GREP find/replace `^[abcd]-`→пусто).**
      1041 совпадение = 1160 параграфов − 4 заголовка − 115 «см.»-строк
      (точное совпадение); 7599 числовых последовательностей идентичны до/
      после — без потерь. **Найден новый вопрос**: 104 параграфа несут
      хвостовой `=N` (уникализатор `Index.Topics`) — 44 заголовка, ВСЕ
      100% с ИДЕНТИЧНЫМИ списками страниц у всех своих `=N`-вариантов
      («великий риши» печатается 4 раза подряд с одним и тем же списком
      страниц). Гипотеза: общий эпитет применён как подстатья к нескольким
      персонажам, а grep-поиск не различает, какое вхождение к кому
      относится — редакционная неоднозначность словника, не баг этой
      сессии. Нужно решение человека (скрыть/объединить/добавить глоссы).
      Остаток: решение по `=N` → `AddAnnotationData.v.3.jsx` → финал.

- [ ] 2026-07-12 (Fable 5 `claude-fable-5`): **Среда переводчика книг 5–7 (автосноски по трудным словам)** — роадмап с 4 рулингами МГ: [`docs/ROADMAP_LEONOV_TRANSLATOR_ENV_RAMAYANA_5_7_2026.md`](docs/ROADMAP_LEONOV_TRANSLATOR_ENV_RAMAYANA_5_7_2026.md). **Wave 0 = ✅ ВЫПОЛНЕНА** (H764, Opus 4.8 `claude-opus-4-8`, [PR #25](https://github.com/gasyoun/RussianRamayana/pull/25) merged — движок трудности + листы в 3 формах + валидация, см. Completed ниже). Wave 1 = инжест санскрита книг 6–7 (H765, Sonnet 5 `claude-sonnet-5`, в SamudraManthanam) — ещё открыта. @DO МГ: письмо Леонову (экспорт лога 499 запросов + издание книг 6–7 + выбор формы после пилота), зеркалировано в GTD.
- [x] 2026-07-10 (H371, Sonnet 5 `claude-sonnet-5`): revival audit run — verdict: repo already
      active (not dormant since 26-06 as the handoff assumed), `roadmap.md` already carries the
      audit-derived wave/decisions/risks structure H371 asked for. Disposition:
      [`docs/H371_REVIVAL_AUDIT_2026-07-10.md`](docs/H371_REVIVAL_AUDIT_2026-07-10.md). No new
      roadmap written (would have forked the existing one); no new handoff minted (the one
      genuine agent-doable item — Litpam-Indexator validator live smoke-run — was already GTD-tracked).
- [ ] Фаза 1 ОТЛОЖЕНА (решение М.Г. 2026-06-13): права на текст Потаповой не урегулированы → IA откладываем. Аудио остаётся на raw.github как есть. Манифест docs/ia-upload.md готов к реактивации, когда/если решится вопрос прав. Чистка истории MP3 тоже откладывается (нет смысла без переноса на IA).
- [x] Фаза 2: публичная HTML-страница «Права и источники» (по docs/RIGHTS.md)
- [ ] Фаза 3 (остаток): Wikidata-item Леонова (после выхода книг V–VI). Кривецкий — minor, отложен (решение М.Г. 2026-06-13). Zenodo DOI → вписать в CITATION.cff.
- [ ] Фаза 5 (остаток): ежегодный Zenodo-снапшот → DOI в CITATION.cff.
- [ ] Параллельно: задачи Q3–Q4 2026 из roadmap.md (расшифровки Леонова, импорт донатов, карта)

## 🚧 Current Work-In-Progress (WIP)

- Фазы 0, 2, 3, 4, 5(CI-JSON) + краудфандинг-rebuild выполнены и запушены в origin/main (2026-06-13). Открыто: IA-загрузка (Фаза 1, блокер — права Потаповой), HTML-страница «Права», Wikidata-item Леонов, Zenodo DOI/снапшот.
- Фаза 5 (2026-06-13): scripts/validate_data.py (parse-check всех data/*.json + schema-валидация); 14 схем в data/schema/ (reports.json — parse-only); job «Validate data JSON» в ci.yml (блокирующий). Негативный тест пройден (битый JSON → exit 1). Нужен jsonschema (CI ставит сам).
- Краудфандинг (2026-06-13): summary.json → два блока onetime{}/monthly{}; support.html — две раздельные секции с graceful zero-state (нули = приглашение, не «0 ₽»); index.html читает onetime.*. ⚠️ М.Г. ОБЕЩАЛ позже: реальные суммы в summary.json + платёжные URL в payment-methods.json (сейчас все 5 = «Скоро», давать нельзя). Инструкция: data/fundraising/README.md.

## ❓ Вопрос к М.Г. (Sanskritist — вероятно знает или есть книга под рукой)

- **Рецензия перевода Гринцера (ЛП).** Не подтверждено по сети, из какого санскритского текста переводил Гринцер тома I–III (Бароды/критическое изд. ИЛИ бомбейская вульгата). В editions.json у всех 5 томов ЛП поставлено `recension: "уточняется"`; в UI НЕ показываем «уточняется», пока не подтвердишь. Потапова — `южная (вульгата)`, со средней уверенностью (по описанию изд.: «традиционный санскритский текст»). Нужна textolog. справка из печатного «От переводчика».
  - **12-07-2026 (H783, Sonnet 5 `claude-sonnet-5`):** появились данные, которые могут помочь ответить — построчное критическое(GRETIL/Барода)↔южное(Gita Supersite) сопоставление книг I–IV с таблицами соответствия сарг, [`CommentaryStrategies/data/edition_comparison/gitasupersite_kandas/`](https://github.com/gasyoun/CommentaryStrategies/tree/main/data/edition_comparison/gitasupersite_kandas). Не применялось к этому вопросу — сверка (совпадает ли нумерация Гринцера с крит. или южн. рядом) не проводилась, отдельная задача.
  - Также запрошено у Серебряного (переводчик кн. IV): [issue #31](https://github.com/gasyoun/RussianRamayana/issues/31) — из какой рецензии переводит/планирует переводить.
- Все канонические ссылки (R.k.s.ś) в comparison-episodes.json — по ЮЖНОЙ рецензии (valmikiramayan.net, теперь Gita Supersite после H783). Если перевод Гринцера/Леонова окажется по критическому изданию, нумерация сарг 3,4,6,8 может сдвинуться — тогда добавить второй ключ canonical_ref_crit.

## ✅ Completed (Recent only)

- [x] 2026-08-14 (H2589, Sonnet 5 `claude-sonnet-5`; crash-recovery delivery — Fable 5
      `claude-fable-5`): **Litpam-Indexator deterministic tooling + Book-I print-readiness
      пилот.** `tools/print_ready.py` + пакет (`repair-workbook`/`audit-idml`/`audit-pdf`/
      `coverage`/`verify-packet`/`conversion-gate`), 20 pytest green. Словник: 42/43
      validator-находок исправлены идемпотентно в `xls/derived/Указатель_к_Рамаяне_1_2_2026_08_12.xlsx`
      (ledger в `artifacts/print-readiness/dictionary/`), 1 WAITING (строка 221). Baseline-2022
      (статический аудит нативного пакета 2022) + живой conversion-2026 экспорт (COM, 442 стр.):
      **gate FAIL** — DFT-I-0002 overset ×3 (вероятно сам Именной указатель), DFT-I-0001
      6 missing links (правдоподобно pre-existing). Оба evidence-пакета `verify-packet` PASS.
      Шаг 6 (пилот) по контракту не запускался. Сессия упала до коммита; доставлено 14-08.
- [x] 2026-08-01 (H2061, Grok 4.5): **DH_ROADMAP Фаза 0 закрыта** — stale-tick
      `.ai_state`/`_meta`/`cover-og` + `resolveAudioUrl()` в `js/utils.js` (ia_url→url),
      wired в audio/index/media. См. changelog 2026-08-01.
- [x] 2026-07-12 (H764, Opus 4.8 `claude-opus-4-8`): **Среда переводчика Wave 0 — пилот на
      Сундараканде, сарги 1–2** ([PR #25](https://github.com/gasyoun/RussianRamayana/pull/25)
      merged). `translator-env/`: движок трудности (`difficulty.py`, 2 яруса — A расхождение
      классиков по семьям основ с частотным потолком, ловит `kālāntaka` не `mahat`; B
      нечастотное+аттестовано) + селф-TM Леонова + дедуп против его заметок/tier-2 +
      подавление глагольных/служебных форм. Листы в 3 формах (`gen_sheets.py` +
      `render_docx.py`): офлайн HTML-обозрение, `.docx` с настоящими Word-сносками (pandoc),
      web-мок reader. Проба DeepSeek (`deepseek_synth.py`) — машинная сводка на 5 шлоках,
      вшита с меткой. Валидация ([`translator-env/VALIDATION_SARGA1.md`](translator-env/VALIDATION_SARGA1.md)):
      difficulty-recall 61.5 %, ~2.5 сноски/шлока; **находка — расхождение рецензий
      аппарат↔корпус (37 % лемм недостижимы)**. ⚠️ Приватность: листы + `data/` в `.gitignore`
      (копирайт Кочергина/переводы/подстрочник); публичная ветка форс-пушнута, чтобы убрать
      ранее закоммиченный `candidates.json`. Сигнал (в) лог-499 — СТАБ до экспорта с машины
      Леонова (@WAITING). Backlog волны 1 — в метадоке валидации.
- [x] 2026-07-10 (H377, Fable 5 `claude-fable-5`): **Litpam-Indexator — кардинальная
      переработка MANUAL + фиксы кода + копилот.** MANUAL.md теперь три части: Часть I
      самодостаточна без видео (блоки «Диалог скрипта»); Часть II — «Пофайловый разбор
      скриптов» (все ~28 `.jsx` + `ForIndex.jsxinc`: назначение/алгоритм/UI/контракты/
      дефекты); Часть III — модернизация (`tools/gen_case_forms.py` pymorphy3;
      `tools/copilot/` — deepseek_common/enrich_dictionary/triage_log/copilot_qa с
      running-cost и бюджетным стопом $20, все проверены живыми вызовами ≈$0.006) +
      ACL-раздел (6 работ ↔ стадии). **7 дефектов кода исправлены** (guardrail снят MG),
      по коммиту на дефект: AddMarker help a…i/^[a-i]-; UseReadyTable автонумерация
      возвращена; pop()-баги ProcNumberLines×2/AddLetter; nQ==2→0 (мёртвая проверка
      панелей 3-4); bounds-guard SkipTheWord; textPointer дефолт «см.»; 14 $.writeln
      AddSeeTopic за флагом debugLog. node --check чист (кроме документированных
      ES3-идиом). **Оператору: прогнать Golden Fish дрилл в InDesign до боевого прогона
      UseReadyTable/ProcNumberLines** (статическая проверка сделана, InDesign — нет).
      MANUAL + усиление процесса.** (D1) `docs/indesign-pipeline/MANUAL.md` дополнен
      шпаргалкой, диаграммой потока данных, таблицей «Симптом→Причина→Лечение»,
      глоссарием, разделом «Окружение», бюджетом времени, **Розеттской таблицей**
      маркеров и взаимными ссылками; три документа (MANUAL/Roadmap/CLAUDE) сведены к
      **канону букв-маркеров `a/b/c/d`** из `ForIndex.jsxinc` (`markLetters`, строка 63) —
      Roadmap-схема `N/G/T/F` помечена как мнемоническое предложение, не факт. (D2)
      Аддитивный read-only `Litpam-Indexator/tools/validate_dictionary.py` (openpyxl, без
      Excel/OLE): прогон на реальном `.xlsx` → **43 находки** (41 `duplicate_forms` —
      преимущественно дубли по регистру, 1 `trailing_semicolon`, 1 `prose_in_forms`);
      выборочно сверены `Пашупати;`, `без тег`, регистр-дубли — реальные. Отчёт:
      `docs/indesign-pipeline/dictionary-validation-report.md`. (D3) Регрессионный дрилл
      дефекта «одной формы» — `…/Golden Fish Story/regression_expected.md` (эталон-свойства
      + буквальный снимок заполняется оператором при первом исправном прогоне в InDesign).
      (D4) расширен список наблюдений по коду в MANUAL. **Guardrail соблюдён**: авторские
      `.jsx`/`.xlsx`/`.indd` не менялись. Доставка — PR с авто-мёрджем.
      **@DECIDE → РЕШЕНО (М.Г., 08-07-2026): не вводить, оставить `a/b/c/d` как есть.**
      Мнемонические буквы-маркеры (`N/G/T/F`) НЕ вводятся; канон `markLetters =
      ["a","b","c","d",…]` из `ForIndex.jsxinc` остаётся. `AddMarker.jsx` не трогаем.

- [x] 2026-07-08: Библиографический раздел (roadmap.md Q3-Q4 item 5) оказался
      УЖЕ построен и опубликован в прошлой сессии (`data/editions.json`,
      `bibliography.html` с BibTeX/CSL-JSON экспортом, слинкован с
      `index.html`, задокументирован в README.md) — просто roadmap.md и
      `docs/content-inventory.md` не были обновлены. Проверено: `python
      scripts/validate_data.py` (18/18 schema-validated), `bibliography.html`
      и `data/editions.json` отдают 200 локально. Отмечено готовым в обоих
      файлах вместо повторной постройки (prior-art check).

- [x] 2026-07-08: `Leitan-Sundarakanda/` (рабочие черновики санскритского текста
      Сундара-канды, изд. Parab 1888 с комментарием Тика, сарги 23-33 + полная
      компиляция) была добавлена одним коммитом без ссылок откуда-либо — вписана
      в сайт/roadmap (H365): `Leitan-Sundarakanda/README.md` — обратная ссылка на
      project.html/roadmap.md; `roadmap.md` — добавлена в список «сверка
      санскрита» + отдельный абзац рядом с Litpam-Indexator (санскритский
      источник книги V); `project.html` — ссылка в секции «Команда проекта»
      (тот же паттерн, что у Litpam-Indexator); `docs/content-inventory.md` —
      новая строка (`internal`, не для публикации как есть).

- [x] 2026-07-07: `Litpam-Indexator/README.md` написан с нуля (Explore-агентом
      картирован весь toolkit: 5-стадийный .jsx-пайплайн InDesign,
      `_Ram_Tag_explorer` Lazarus-утилита, `Tags/ram_tags.txt`,
      `xls/Указатель_*.xlsx`, инварианты из `CLAUDE.md`/`ForIndex.jsxinc`,
      use cases). Живой сайт на индексатор раньше НЕ ссылался — исправлено:
      добавлена статическая ссылка в `project.html` (секция «Команда
      проекта», вне esc()-экранируемого `people.json`) + 2 ссылки в
      `roadmap.md` (список доп. этапов + строка «за указатели отвечает
      М.Г.»). Не закоммичено — на подтверждении.

- [x] 2026-06-26: Фаза 2 public rights page — added `rights.html` sourced from
      `docs/RIGHTS.md`, linked it from `index.html`, added it to `sitemap.xml`,
      and documented it in `README.md`. Validation: `python scripts\validate_data.py`
      → 18 schema-validated, 0 failed.

- [x] 2026-06-26: `reports.json` schema verified already present —
      `data/schema/reports.schema.json` matches the current fundraising report shape and
      is wired automatically by `scripts/validate_data.py`; validation now reports
      18 schema-validated, 0 parse-only, 0 failed.

- [x] 2026-06-13: Print stylesheet — исправлен @media print (прятал <header>→терялся заголовок h1 при печати; добавлены новые элементы chrome, показ URL источников в .sources для офлайн-цитирования, break-inside для новых карточек). Также проверено: prerender реально запёк контент (dist/ + живой сайт отдают текст без JS).
- [x] 2026-06-13: Ревью-сессия (4 трека, автономно). (1) Cleanups: CLAUDE.md «no build»→корректно (dev build-free + опциональный prerender); reports.schema.json (18/18 schema-validated); deploy.yml paths-ignore для *.md/docs (docs-коммиты не гоняют 5-мин prerender). (2) Security-ревью клиентского кода: XSS чисто (esc на &<>", safeUrl только https, rel=noopener); НАХОДКА — калитка drafts.html не контроль доступа (drafts.json качается напрямую, хеш в исходнике без соли, PLACEHOLDER открывает любым вводом) → коммент в коде + SECURITY.md; правило «реальный подписочный текст не в статике». Контент сейчас заглушки. (3) A11y: глобальный :focus-visible (не было — фокус был невидим), prefers-reduced-motion, lang=sa/sa-Latn на санскрит-полях (characters/kandas/map), aria-label на пароль; img нет (нет alt-проблем), html lang везде. (4) Responsive проверен @375px: characters(SVG), карта(stack), translations — 0px overflow, правок не нужно.
- [x] 2026-06-13: Prerender/SSG (Full, выбор М.Г.). Идемпотентные рендеры (container.innerHTML='' перед populate) на 11 страницах → пере-гидратация без дублей. scripts/prerender.py (Playwright: грузит каждую страницу с её СОБСТВЕННЫМ JS на лок-сервере, сериализует DOM в dist/; drafts.html ИСКЛЮЧЁН — подписочный контент не печём). .github/workflows/deploy.yml (prerender → configure-pages → upload-pages-artifact → deploy-pages); dist/ в .gitignore. Локально проверено: 16 страниц испечены, контент статичен, при гидратации дублей НЕТ. Pages source переключён branch→GitHub Actions (gh api build_type=workflow); deploy.yml зелёный (Pre-render + Deploy). ПРОД проверен curl без JS: translations/kandas/characters отдают baked-контент в сыром HTML (граф 13 узлов в статике), drafts НЕ испечён (paywall цел). SSG живой.
- [x] 2026-06-13: Opus-сессия #3 — 3 трека (QA + ссылки + README). (1) Адверсариальная проверка нового научного контента (агент, независимая сверка с источниками): characters.json (20 QID + связи), kandas.json (сарги), 4 шлоки, recensions/reception — всё ПОДТВЕРЖДЕНО, кроме 1 опечатки: IAST cāraṇacarite→cāraṇācarite (длинная ā) в Сундара 5.1.1 — исправлено. (2) scripts/check_links.py (stdlib, UA, IDNA/percent-encode кириллицы, skip шаблонов {z}/SVG-ns/инфра-хостов, bot-hostile→review) + неблокирующий job «Link health» в ci.yml; нашёл 2 битых: Princeton URL (404, схема изменилась) и valmiki.iitk.ac.in (000) → де-линкованы в recensions.html (цитата без ссылки); 70 ссылок OK. (3) README.md обновлён под весь стек (16 страниц, data/+схемы+export, FAIR/LOD, scripts, CI, docs); все 13 ссылок проверены. 18 data-файлов, 17 schema-validated.
- [x] 2026-06-13: Opus-сессия #2 — 4 трека. (1) Аудит консистентности: исправлены project.html «Викторович»→«Владимирович» (×2), people.json «Мартин»→«Марцис» Гасунс (имя руководителя!), audio.json «В. Потапова»→«В. А. Потапова»; проверены все 13 внутр. ссылок (резолвятся), орфография имён/дат. (2) Карта связей: data/characters.json (20 героев, QID сверены агентом, рёбра родство/брак/союз/вражда) + characters.html (SVG-граф 13 узлов/17 рёбер по фикс-раскладке + карточки по фракциям). (3) FAIR: datapackage.json (Frictionless, 21 ресурс, CC BY 4.0, все пути проверены). (4) kandas.html «Структура эпоса» из data/kandas.json (7 канд, сарги сверены по valmikiramayan.net, Бала+Уттара=позднее ядро). Схемы characters/kandas добавлены; 18 файлов, 17 schema-validated. Обе страницы слинкованы с index. Проверено в браузере, 0 ошибок консоли.
- [x] 2026-06-13: Opus-сессия «что дальше» — 4 трека. (1) Сверка каталогов translations.json↔editions.json: убраны противоречия (Кн.IV in-press 2025→blocked; V/VI статусы; Потапова «полный»→сокращ. ~6000/48000; имена; potapova-full→potapova). editions.json = каноническая библиозапись, translations.json = витрина по тем же id; словарь статусов унифицирован; отношение задокументировано в schema/README. (2) Санскрит в 7 пустых compare-эпизодах: IAST+деванагари (сверено по valmikiramayan.net, URL 200-проверены) + «Дословно» (мой рабочий подстрочник, ≠ продукт Леонова) + плейсхолдер «Литературный перевод»; canonical_ref уточнён до шлоки; все 8 эпизодов «Готов». (3) recensions.html «Рецензии и текст» (сев./южн., вульгата+Говиндараджа, крит. изд. Бароды 1960–75, ~24k шлок, Бала/Уттара поздние). (4) reception.html «Рамаяна в России» (Коссович→Эрман/Тёмкин 1965→Потапова БВЛ 1974/отд.1986→Гринцер ЛП 2006/2014→Серебряный/Леонов). Обе страницы слинкованы с index. Проверено в браузере (JSON-LD валиден, 0 ошибок). NB: research-агент не смог внешне подтвердить продолжение Серебряный/Леонов (ожидаемо — не издано) — поданы как forthcoming. Поправка: перевод Потаповой впервые вышел в БВЛ 1974, не 1986 (1986 = отд. изд.).
- [x] 2026-06-13: «Об карте» инфо-бокс + газетир. (1) ramayana-map-leaflet.html: модалка-оверлей (кнопка «ⓘ Об отождествлениях», Esc/клик-вне-закрытия) — рендерит localization_note + спорные отождествления (Митхила/Кишкиндха/Ланка из STOPS.alt_identification + источники) + футер про южную рецензию. (2) Газетир: data/gazetteer-extra.json (Махендрагири, Пампа/Шабари — вне маршрута) + scripts/export_gazetteer.py → data/export/gazetteer.geojson (12 мест: 10 on-route дедуп + 2 extra; Point-слой). Схема gazetteer-extra добавлена. Ссылка «Газетир (GeoJSON)» в сайдбаре. Проверено в браузере, 0 ошибок.
- [x] 2026-06-13: GeoJSON-экспорт маршрута + Дандака как полигон. scripts/export_geojson.py → data/export/rama-route.geojson (FeatureCollection RFC 7946: 11 Point + 1 Polygon[Дандака] + 1 LineString[маршрут]; localization_note как foreign member; [lng,lat]). Дандака: +is_region+polygon в rama-route.json и схеме; карта рисует L.polygon (зелёный пунктир, tooltip-метка) вместо булавки для is_region. Ссылка «Скачать GeoJSON» в сайдбаре. Проверено в браузере (полигон над Деканом, GeoJSON отдаётся 200, кольцо замкнуто), 0 ошибок консоли.
- [x] 2026-06-13: Углубление географии (LOD + канонические локусы + спорные отождествления + детализация маршрута). rama-route.json 9→11 остановок (+Шрингаверапура, +Праяга/Бхарадваджа; Махендрагири НЕ добавлен — спорный пик + ломает полилинию; Шабари/Пампа — со-локализована с Кишкиндхой). На каждой точке: name_iast, name_devanagari, wikidata (мифич.), modern_wikidata (совр. место), canonical_ref, source, alt_identification+alt_source (Ланка: Кибе/Гупта/Агаше; Кишкиндха). segments пересозданы (10 прямых). UI карты: d-sanskrit, d-meta (canonical + Викиданные-ссылка, QID regex-guard), d-alt («⚠ Спорно»), динамичный счётчик остановок. Схема rama-route обновлена. Проверено в браузере, 0 ошибок консоли. QID сверены агентом по Wikidata.

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- Решения владельца (2026-06-12): портал, не корпус; аудио → archive.org; права на аудио 1986 очищены письменно; полный LOD-каркас.
- `bin/` (ffmpeg, 652 MB) в .gitignore — НЕ в git, не трогать историю ради него. `_meta/` теперь тоже в .gitignore (это RuWritingStyles-материалы, не Рамаяна); из ИСТОРИИ PNG ещё не вычищены — вместе с MP3 в Фазе 1.
- MP3 всех 7 книг ЕСТЬ локально (V–VI просто в .gitignore) — Яндекс Диск не нужен.
- audio.json: у книг 5–6 был мёртвый raw.github-URL (файлы не в git) — заменён на "", рендер audio.html это переживает (guard на status+url).
- Force-push после filter-repo: снять branch protection, предупредить о re-clone.
- Аудио-URL зашиты как raw.githubusercontent.com в data/audio.json — единая точка замены.
- Wikidata (ПРОВЕРЕНО 2026-06-12): Гринцер = Q4149672 (НЕ Q4147525!), Потапова Q15720383, Серебряный Q4417419, Эрман Q4532534 (ум. 2017), Нарайян Q334252, Вальмики Q715607, Рамаяна Q37293, ЛП Q4263826. Леонов и Кривецкий — items НЕТ (создание = внешнее действие, нужно согласие М.Г.).
- Леонов = «Максим Владимирович» (подтверждено М.Г. 2026-06-13), исправлено в people.json.
- Push: HTTPS git напрямую падал (schannel SSL handshake); лечится `gh auth setup-git` (gh залогинен, token scope=repo). Force-push для filter-repo (Фаза 1) — отдельно, со снятием branch protection.

## ✅ Completed (Recent only)

- [x] 2026-06-12: DH-аудит репозитория; docs/DH_ROADMAP.md (5 фаз); решения по 4 ключевым вопросам зафиксированы.
- [x] 2026-06-12: Фаза 0 — `_meta/` untracked+gitignored (RuWritingStyles-скрапы); создан отсутствовавший web-src/cover-og.png (1200×630, был битый OG у 13 страниц); scripts/audio_inventory.py.
- [x] 2026-06-12: IA-подготовка — audio.json обогащён (file, size_bytes, sha256, ia_identifier), docs/ia-upload.md (манифест, метаданные item, команды ia CLI, чек-лист после загрузки).
- [x] 2026-06-12: Фаза 2 — LICENSE-data.md (CC BY 4.0 на data/+docs), docs/RIGHTS.md (реестр прав, 10 позиций, 2 открытых вопроса), `rights` в audio/videos/drafts.json, раздел «Лицензии и права» в README. Дата разрешения Кривецкого: 2026.
- [x] 2026-06-12: атрибуция записи 1986 подтверждена М.Г.: перевод В. Потаповой, частная запись Е. Кривецкого (не ОРС, не «Мелодия»). audio.json +translator; audio.html — исправлена атрибуция в meta/шапке. Права на запись (Кривецкий) ≠ права на текст (Потапова) — учесть в RIGHTS.md Фазы 2.
